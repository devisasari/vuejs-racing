import { mount } from "@vue/test-utils";
import { describe, it, expect } from "vitest";
import App from "../src/App.vue";

// I tried to test the App.vue component, but I didn't get it to work.
// I will learn how to test components with vitest and test the App.vue component later.